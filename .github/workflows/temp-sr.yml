name: temp-sr
on: workflow_dispatch
jobs:
  lesen:
    runs-on: ubuntu-latest
    steps:
      - env:
          SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
        run: |
          Q() { python3 -c "import json,sys; print(json.dumps({'query': sys.argv[1]}))" "$1" | curl -sS -X POST "https://api.supabase.com/v1/projects/tdehljzmqwmfgfoyyoee/database/query" -H "Authorization: Bearer ${SUPABASE_ACCESS_TOKEN}" -H "Content-Type: application/json" -d @-; }
          echo "KOPF>>>"
          Q "SELECT to_jsonb(i) - 'kunde_adresse' - 'kunde_email' - 'kunde_telefon' - 'lieferadresse' AS zeile FROM invoices i WHERE id='af88493c-295f-426f-a682-f2e8b9857a7b';"
          echo ""
          echo "ZAHLUNGEN>>>"
          Q "SELECT * FROM invoice_payments WHERE invoice_id IN ('7f930e08-f142-44e9-8788-966a3d60f3a0','af88493c-295f-426f-a682-f2e8b9857a7b');"
          echo ""
          echo "TEXTE>>>"
          Q "SELECT key, left(value, 200) AS value FROM document_texts WHERE key ILIKE '%closing%' OR key ILIKE '%zahl%' ORDER BY key;" 2>/dev/null
          Q "SELECT column_name FROM information_schema.columns WHERE table_name='document_texts';"
